import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Register",
  description:
    "Register or enquire for Bijalsangnaach Kathak classes — Beginner to Advanced, in-person in New Jersey or online worldwide.",
};

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return children;
}
